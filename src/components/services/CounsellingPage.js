import React, {PropTypes} from 'react';
<div></div>
const CounsellingPage = ({props}) => {
    return ( <div> </div>

    );
};

CounsellingPage.propTypes = {
    //myProp: PropTypes.string.isRequired
};
CounsellingPage.defaultProps = {
    //myProp: ''
};


export default CounsellingPage;
