import React, {PropTypes} from 'react';
<div></div>
const GeneralTraining = ({props}) => {
    return ( <div> </div>

    );
};

GeneralTraining.propTypes = {
    //myProp: PropTypes.string.isRequired
};
GeneralTraining.defaultProps = {
    //myProp: ''
};


export default GeneralTraining;
