import React, {PropTypes} from 'react';
<div></div>
const GedPage = ({props}) => {
    return ( <div> </div>

    );
};

GedPage.propTypes = {
    //myProp: PropTypes.string.isRequired
};
GedPage.defaultProps = {
    //myProp: ''
};


export default GedPage;
