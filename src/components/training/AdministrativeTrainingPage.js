import React, {PropTypes} from 'react';
<div></div>
const AdministrativeTrainingPage = ({props}) => {
    return ( <div> </div>

    );
};

AdministrativeTrainingPage.propTypes = {
    //myProp: PropTypes.string.isRequired
};
AdministrativeTrainingPage.defaultProps = {
    //myProp: ''
};


export default AdministrativeTrainingPage;
