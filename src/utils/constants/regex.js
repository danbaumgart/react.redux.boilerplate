const REGEX = {
    WHITESPACE: '\s',
    TRAILING: '$',
    LEADING: '^',
    IGNORE_CASE: 'i',
    GLOBAL: 'g'
};
export const {WHITESPACE, TRAILING, LEADING, IGNORE_CASE, GLOBAL} = REGEX;
export default REGEX;
